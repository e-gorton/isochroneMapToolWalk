# No API keys are required for the no-key indicative bus catchment version.
# Cloudflare Worker proxies public OpenStreetMap/Overpass, Nominatim, MapIt and Valhalla endpoints.

# Optional BODS timetable mode. Set in Cloudflare as a secret, not in front-end code.
BODS_API_KEY=
